define(['/app/controllers/module.js'], function (controllers) {
  'use strict';
  controllers.controller('SplashController', ['$scope',
    function($scope) {

    console.debug('TODO: Authenticate and redirect to logout/login if not authenticated.');

  }]);
});
